{
  "name": "database",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/database/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/database/**/*.ts"]
      }
    },
    "typeorm": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "typeorm-ts-node-commonjs",
        "cwd": "packages/database",
        "envFile": ".env"
      }
    },
    "migration:generate": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "nx run database:typeorm -- migration:generate --dataSource src/data-source.ts src/migration ",
        "cwd": "packages/database"
      }
    },
    "migration:run": {
      "executor": "nx:run-commands",
      "outputs": [],
      "options": {
        "command": "nx run database:typeorm migration:run --dataSource src/data-source.ts",
        "cwd": "packages/database"
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/packages/database"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/packages/database"
      }
    }
  },
  "tags": ["server"]
}
